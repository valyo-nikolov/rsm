package rsm.third_task;

import org.apache.commons.io.FileUtils;

import java.io.File;
import java.io.IOException;
import java.io.InputStream;

public class Upload {

    void doUpload(String key, InputStream data, int size) throws IOException {
//        File rootDirectory = new File("");
//        String dirUpload = rootDirectory.getAbsolutePath() + "/target/classes/upload";
//
//        File dest = new File(dirUpload);
//        try {
//            FileUtils.copyInputStreamToFile(data, dest);
//        } catch (IOException e) {
//            e.printStackTrace();
//        }
    }

}
